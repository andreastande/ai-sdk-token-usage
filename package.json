{
  "name": "ai-sdk-token-usage-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r --filter ai-sdk-token-usage build",
    "dev": "pnpm -r --parallel --stream dev",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.5",
    "@changesets/cli": "^2.29.7"
  }
}
